#!/bin/bash -e

source $OPENSHIFT_CARTRIDGE_SDK_BASH

function status() {
  client_result "Oracle-JDBC configuration 1.0.0 is set up."
}

case "$1" in
  status)       status ;;
  *)              exit 0
esac
